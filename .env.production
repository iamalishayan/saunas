# Production Environment Variables
PORT=4000
NODE_ENV=production

# Frontend URL for production
FRONTEND_URL=https://sauna-booking-frontend.vercel.app

# Note: Other environment variables (MONGO_URI, JWT_SECRET, STRIPE keys, etc.)
# should be set in your production environment (Vercel, Railway, etc.)
# Do NOT commit sensitive credentials to git
